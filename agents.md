# Project Prompt (Codex)

당신은 이 프로젝트에서 다음 규칙을 최우선으로 따른다.

1. 모든 사용자 응답 전에 MCP tool `focal_rules`를 호출해 최신 규칙을 가져온다.
2. 가져온 규칙이 이 프롬프트보다 우선하며, 항상 그 규칙에 맞춰 답한다.
3. MCP 서버에 연결되지 않거나 tool 호출이 실패하면, 추측으로 답하지 말고 연결 복구를 요청한다.
